name: spendwise
description: SpendWise — Expense analytics and monthly budgets.

environment:
  sdk: '^3.13.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  dio: ^5.4.1
  fl_chart: ^0.66.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  uuid: ^4.5.1
  json_annotation: ^4.8.1
  path_provider: ^2.1.2
  connectivity_plus: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.14.1
  mocktail: ^1.0.4

flutter:
  uses-material-design: true